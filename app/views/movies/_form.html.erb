<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: "form-control form-control-lg"%>
  </div>

  <div class="field form-group">
    <%= form.label :description %>
    <%= form.text_area :description, class: "form-control form-control-lg" %>
  </div>

  <div class="field form-group">
    <%= form.label :duration %>
    <%= form.number_field :duration, class: "form-control form-control-lg" %>
  </div>

  <div class="actions form-group">
    <%= form.submit class: "btn btn-primary btn-lg" %>
  </div>
<% end %>
